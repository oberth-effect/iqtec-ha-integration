{
  "config": {
    "abort": {
      "already_configured": "Device is already configured"
    },
    "error": {
      "cannot_connect": "Failed to connect",
      "unknown": "Unexpected error"
    },
    "step": {
      "user": {
        "data": {
          "correction_timeout": "Manual climate timeout (5 minute intervals)",
          "cover_use_short_tilt": "Covers use short tilt to open",
          "host": "Host"
        },
        "data_description": {
          "correction_timeout": "How long a manually set temperature is held, in 5 minute intervals. 24 is two hours.",
          "cover_use_short_tilt": "Use the short tilt command instead of the full tilt cycle when opening a blind's slats.",
          "host": "Hostname or IP address of the IQtec controller, optionally with a port."
        }
      }
    }
  }
}
