[pytest]
testpaths = tests
# The repository root must be importable for `from custom_components.iqtec ...`
# when pytest is started as a console script rather than `python -m pytest`.
pythonpath = .
asyncio_mode = auto
